/**
 * Color Scheme Hook
 * 
 * Re-exports the useColorScheme hook from React Native.
 * This hook provides access to the device's current color scheme
 * (light or dark mode) and automatically updates when the user
 * changes their system preferences.
 * 
 * Usage:
 * const colorScheme = useColorScheme();
 * // Returns 'light', 'dark', or null
 */

export { useColorScheme } from 'react-native';
